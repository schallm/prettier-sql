# Source — compiled to dist/
src/
tsconfig.json

# Packaging script (only needed for building, not at runtime)
scripts/

# Bundled package metadata (node_modules ARE included; .npmrc, package.json are not needed)
bundled/.npmrc
bundled/package.json
bundled/package-lock.json

# Dev artifacts
.github/
*.vsix
node_modules/

# Keep
!dist/**
!bundled/format.mjs
!bundled/node_modules/**
