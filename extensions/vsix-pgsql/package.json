{
    "name": "prettier-pgsql-vsix",
    "private": true,
    "type": "module",
    "scripts": {
        "generate-notices": "node ../shared/generate-notices.mjs --dialect PostgreSQL",
        "test": "node --test test/*.test.mjs"
    }
}
