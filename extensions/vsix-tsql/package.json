{
    "name": "prettier-tsql-vsix",
    "private": true,
    "type": "module",
    "scripts": {
        "generate-notices": "node ../shared/generate-notices.mjs --dialect T-SQL",
        "test": "node --test test/*.test.mjs"
    }
}
