create table orders (id int not null identity(1,1), UserId int not null, Total decimal(18,2) not null, Status nvarchar(50) not null default 'pending', CreatedAt datetime2 not null default getdate(), constraint pk_orders primary key (id), constraint fk_orders_users foreign key (UserId) references users (id), constraint ck_orders_total check (Total >= 0))
