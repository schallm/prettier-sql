with available_books as (select Id, Title from Books where InStock = 1), book_orders as (select BookId, count(*) as OrderCount from OrderItems group by BookId) select available_books.Title, book_orders.OrderCount from available_books inner join book_orders on available_books.Id = book_orders.BookId order by book_orders.OrderCount desc
